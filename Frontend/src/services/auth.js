import http from './http'

class Auth {
  constructor() {
    this.authenticated = false
  }

  // login(username, password) {
  //   http.authenticate(username, password).then
  // }
}

export default new Auth()
